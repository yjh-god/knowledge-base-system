# Cursor AI 代码生成规则（精简版 + 完整版）

## 新手精简必看（强制 + 推荐）

### 一、强制规则（每个项目都要做到）

1. **编码与缩进**
   - 使用 UTF-8 编码
   - 缩进统一 4 个空格，禁止使用 Tab
2. **命名规范**
   - 变量/函数：小驼峰，例如 `userName`、`getUserList`
   - 常量：全大写下划线，例如 `API_BASE_URL`、`MAX_PAGE_SIZE`
   - 组件/类：大驼峰，例如 `UserList.vue`
   - 文件/目录：短横线，例如 `user-list.vue`
3. **接口返回格式**
   - 后端接口统一返回结构：
     ```json
     {
       "code": 200,
       "msg": "提示信息",
       "data": {}
     }
     ```
   - 状态码约定：200 成功；401 未登录/Token 无效或过期；403 无权限；404 资源不存在；500 服务器错误（msg 明确提示，不暴露敏感信息）
4. **列表与分页**
   - 列表类接口必须分页，禁止一次返回全表数据
   - 统一参数：page（页码）、pageSize（每页条数），pageSize 上限建议 100
5. **安全红线（绝对不能做）**
   - 禁止在代码里写明文账号、密码、IP、端口（统一使用 `.env`）
   - 禁止使用 `v-html` 渲染用户输入内容
   - 禁止明文存储密码（必须加密）
6. **错误处理与提示**
   - 后端所有接口必须 `try/catch`，发生错误要返回 `code != 200` 和明确的 `msg`
   - 前端请求必须有 Loading、错误提示（比如 `ElMessage.error`）

### 二、推荐规则（有精力就按这个做）

1. **登录与 Token**
   - 登录成功后，后端返回 JWT Token
   - 前端把 Token 保存到 `localStorage` 或 `pinia`
   - 每次请求在请求头里带上：
     ```js
     headers: {
       Authorization: "Bearer " + token;
     }
     ```
2. **路由守卫（前端）**
   - 未登录（没有 Token）时，自动跳转到登录页
   - 已登录时，禁止再访问登录页
3. **基础权限**
   - 至少区分：管理员 / 普通用户
   - 普通用户只能看到和自己相关的数据

### 三、进阶目标（以后慢慢补上）

1. **完整权限系统**：接口级权限 + 数据级权限
2. **安全体系**：防 SQL 注入、防 XSS、防暴力破解、接口限流
3. **日志体系**：登录日志、操作日志、错误日志
4. **项目结构优化**：前后端部署分离（前端静态、后端独立服务）、模块化目录

---

## 下面是完整详细版规则（进阶可选，和上面的精简版同一套思想）

技术栈：Vue3 + Element Plus + Icon 图标库 + ECharts 图表库（若使用 Tailwind CSS 则按 Tailwind 规则）
后端：Node.js(Express) + SQL Server
安全：登录验证、JWT、权限、数据加密、接口安全、防SQL注入、XSS防护

## 一、通用强制规则（所有代码必须遵守）

1. 编码统一 UTF-8，缩进 4 空格，禁止使用 Tab。
2. 命名规范：
   - 变量/函数：小驼峰 userName、getUserList
   - 常量：全大写下划线 API_BASE_URL、MAX_PAGE_SIZE
   - 组件/类：大驼峰 UserList.vue
   - 文件/目录：短横线 user-list.vue
   - 数据库表/字段：下划线，表名统一单数（如 user、task），字段如 user_name、create_time
3. 注释：核心逻辑、接口说明、复杂业务、安全相关必须中文注释；简单一目了然的代码可依赖命名与 JSDoc，不必每行都注。
4. 必须给出运行步骤、安装命令、配置说明。
5. 禁止硬编码 IP、端口、账号、密码，全部使用 .env 环境变量。
6. 所有接口必须做异常捕获，前端必须做 Loading 和错误提示。

---

## 二、前端完整规则（含登录、权限、图标、图表）

### 1. Vue3 通用规则

- 使用 <script setup> 语法
- 页面放在 views，公共组件放 components
- 工具类放 utils，请求放 request.js
- 路由使用 Vue Router，支持路由守卫

### 2. Element Plus 规则

- 使用官方组件：表单、表格、弹窗、上传、分页、菜单、抽屉
- 表单必须做非空、格式、长度校验
- 按钮统一使用 type="primary"、"default"、"danger"

### 3. Tailwind CSS 规则（若项目使用 Tailwind 则遵守）

- 优先使用原子类，不手写冗余 CSS
- 必须做响应式，适配手机、平板、电脑
- 常用：w-full h-full flex justify-center items-center

### 4. 图标库规则

- 优先使用 Element Plus 内置图标（User、Lock、Home、Setting 等）
- 图标大小统一 16px / 20px / 24px
- 必须和文字垂直居中

### 5. 图表库 ECharts 规则

- 支持折线图、柱状图、饼图、环形图
- 图表必须自适应窗口大小变化
- 加载时显示 Loading，无数据时显示空状态
- 图表数据统一从后端接口获取

### 6. 登录 & 权限验证（前端完整规则）【本次新增】

- 必须包含：登录页、验证码（可选）、记住密码、退出登录
- 登录流程：
  1. 输入用户名密码
  2. 请求登录接口
  3. 获取 Token
  4. 存储 Token（localStorage / pinia）
  5. 跳转首页
- 退出登录：清除 Token + 清空用户信息 + 跳转到登录页
- 路由守卫：
  - 未登录 → 自动跳登录页
  - 已登录 → 禁止回登录页
- 请求头统一携带 Token：
  headers: {
  Authorization: 'Bearer ' + token
  }

### 7. 前端安全规则【本次新增】

- 禁止存储明文密码、明文密钥
- 禁止使用 v-html 渲染用户输入内容（防 XSS）
- 所有用户输入必须做过滤
- 敏感页面（个人中心、设置）必须做登录判断

---

## 三、后端完整规则（Node.js + Express + SQL Server）

### 1. 项目结构

project-root/ # 项目根目录
├── frontend/ # 前端工程（Vue3）
│ ├── public/ # 静态资源（无需打包）
│ ├── src/
│ │ ├── assets/ # 图片 / 字体 / 图标等静态资源
│ │ ├── components/# 通用组件（全局注册）
│ │ ├── composables/# 组合式函数（复用逻辑）
│ │ ├── router/ # 路由配置（含守卫）
│ │ ├── store/ # Pinia 状态管理（含持久化）
│ │ ├── utils/ # 工具函数（请求 / 校验 / 加密）
│ │ ├── views/ # 页面组件（登录 / 首页 / 报表等）
│ │ ├── App.vue # 根组件
│ │ └── main.js # 入口文件
│ ├── .env.development # 前端开发环境变量
│ ├── .env.production # 前端生产环境变量
│ ├── package.json # 前端依赖
│ └── vite.config.js # 前端构建配置（Vite）
├── backend/ # 后端工程（Node.js+Express）
│ ├── config/ # 配置文件（数据库 / JWT / 跨域）
│ ├── controllers/ # 控制器（接收请求 / 返回响应）
│ ├── middleware/ # 中间件（鉴权 / 日志 / 异常 / 限流）
│ ├── models/ # 数据模型（SQL Server 映射）
│ ├── routes/ # 路由（接口定义）
│ ├── services/ # 业务逻辑层（核心逻辑）
│ ├── utils/ # 工具函数（加密 / 日志 / 文件处理）
│ ├── uploads/ # 文件上传目录（需配置权限）
│ ├── app.js # 后端入口文件
│ ├── .env # 后端环境变量
│ └── package.json # 后端依赖
├── docker-compose.yml # 容器化部署配置（可选）
└── README.md # 项目说明（启动 / 部署 / 注意事项）

### 2. 接口统一返回格式

{
"code": 200, // 200成功 401未登录/Token无效 403无权限 404不存在 500服务器错误
"msg": "提示信息",
"data": 数据
}

- 列表接口必须分页，参数统一 page、pageSize，pageSize 上限建议 100，禁止一次返回全表。
- package.json 依赖尽量写死版本号（不用 ^ 或 ~），避免日后 npm install 拉断。

### 3. 数据库 SQL Server 规则

- 使用连接池，禁止频繁创建关闭连接
- 所有 SQL 使用参数化查询（防注入）
- 密码必须加密存储（bcrypt / md5 + 盐）
- 增删改必须使用事务
- 查询必须加 TOP 限制，禁止全表扫描
- 敏感字段必须加密存储

### 4. 登录验证 & JWT 规则【本次核心新增】

- 登录接口：
  - 校验用户名密码
  - 密码对比加密后的值
  - 生成 JWT Token 返回前端
- Token 规则：
  - 有效期可配置；开发阶段可设长（如 10 天）方便调试，生产环境建议 24 小时，长期登录可用 refresh token 或「记住我」
  - 包含 userId、username、role 角色
- 鉴权中间件：
  - 所有需要登录的接口必须验证 Token
  - 无效/过期 → 返回 401 → 前端跳登录页

### 5. 权限控制规则【本次新增】

- 角色分为：admin / 普通用户
- 接口级别权限：
  - 超级管理员可访问所有接口
  - 普通用户只能访问自身数据
- 数据级别权限：
  - 用户只能查看/修改自己创建的数据
  - 禁止越权查看他人数据

### 6. 接口安全 & 数据安全【本次新增】

- 防 SQL 注入：必须用参数化查询
- 防 XSS：用户输入过滤特殊字符
- 防越权：接口必须校验用户是否有权限
- 防暴力破解：同一账号连续失败 5 次则锁定 10 分钟（或提示「账号已锁定，请稍后再试」），并记录登录失败日志（账号、IP、时间）
- 密码强度：6~16 位，至少包含字母+数字
- 接口限流：登录/注册/找回密码等敏感接口同一 IP 每分钟最多 5~10 次；普通业务接口同一 IP 每分钟最多 100 次
- 日志：记录登录日志、操作日志、错误日志

---

## 四、完整安全体系（强制遵守）

1. 登录安全：密码加密、验证码、失败锁定、Token 过期
2. 传输安全：所有请求使用 Token 鉴权
3. 接口安全：鉴权、限流、权限判断
4. 数据库安全：防注入、加密、事务、最小权限
5. 前端安全：防XSS、禁止明文存储、路由守卫
6. 文件安全：上传校验格式、大小、重命名
7. 日志安全：登录日志、操作日志、错误日志

---

## 五、你以后开发任何系统都通用的标准流程

1. 登录 → 获取 Token
2. 前端存储 Token
3. 请求头自动带 Token
4. 后端验证 Token
5. 判断权限
6. 操作数据
7. 返回统一格式
8. 前端异常处理 & 提示

---

## 六、回复与交付规则（节省时间）
1. **完成修改后**：不要整理大段代码块、逐行引用贴给用户；直接说明「已按 xxx 改好」或「已改：文件 + 要点」即可。
2. **需要说明时**：用一两句话概括改动点或影响范围，不贴完整代码。
3. **仅当用户明确要求**「贴出修改」「给出 diff」「列出改动」时，再提供代码片段或路径。

---

## 七、SQL Server 连接（开发/调试）
- **说明**：连接信息仅写在 **`backend/.env`**（或根目录 `.env`，按项目约定），**禁止**在规则文件或仓库中写死账号口令或内网 IP。
- **数据库操作约定**：涉及「增加表/字段」「读取测试记录」等，使用当前环境的 `.env` 配置自行连接验证。

**全自动原则**：尽量全自动完成需求，能自己查代码、改代码、跑命令、验证的步骤都直接做，不要中途停下来向用户确认或询问「要不要这样改」；只有确实缺信息（如业务含义不清、多义选择）时才询问。

**脚本与命令**：所有脚本（如测试、检查、构建、数据库校验等）需要运行时直接执行，无需先征得用户确认。

**本机 / 远程执行约定**：本机只要是为当前项目服务的命令或脚本（如 npm install、构建、config-port.js、PM2 部署/管理脚本等），助手可以直接在本机静默执行，不要弹多余窗口或让用户点确认；要给「其它电脑/服务器」用的一键部署脚本或文档，则只修改脚本/文档内容，不在本机反复运行这些部署脚本。
