# 品牌资源

- **标准横版**：`logo.png`（已入库：暗红图形 + 黑字 + 灰阴影）。实现见 PRD §5.7、§5.7.1。
- **深色顶栏（可选）**：若 UI 使用连续深色/暗红导航条，请向设计索要 **`logo-mono-light.png`**（反白/单色稿）并放在本目录，避免黑字 Logo 压在深底上。
- 前端可从本目录复制到 `public/logo.png`，或 `import` 本路径。

请勿在仓库根目录再维护第二份 `logo.png`，以本目录为唯一源。
